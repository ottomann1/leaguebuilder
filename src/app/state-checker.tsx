"use client"

export default function StateChecker(){
    return(
      <p>this will be implemented later it should be something that handles when user is in lobby. probably a timer that revalidates the mainpage.</p>
    )
}
